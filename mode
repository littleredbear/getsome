
关于排列模型：
将排列作为单独的一个类分离出来，以需要进行排列的元素集合作为参数传入，如果传入的是一个空的列表，也可以对这个列表进行填充，返回指定排列的结果。如果传入的列表是一个元素的集合，则直接对元素进行排列（定位）。

关于排列模型思考：
将排列脱离出来可以使排列的代码块重复利用率增加，在最坏的情况下，也只是不利用。
在性能上，脱离出来之后，也可以直接对元素进行排列，故不会有本质上的影响。
